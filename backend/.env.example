# Deja DATABASE_URL vacío para usar SQLite local (sin instalación).
# Para PostgreSQL (Neon/Supabase/local) pega tu URL:
# DATABASE_URL=postgresql://user:password@host:5432/taskflow
DATABASE_URL=
SECRET_KEY=cambia-esta-clave-por-una-segura
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7
